# Transcripts

Generate the two required transcripts using the actual Groq API before submission:

```bash
python src/main.py --save transcripts/transcript_1_approved.md
python src/main.py --weak --save transcripts/transcript_2_rejected.md
```

Do not replace these with hand-written or simulated LLM transcripts; the files should record actual runs.
